import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { OrderFinalPage } from './order-final';

@NgModule({
  declarations: [
    OrderFinalPage,
  ],
  imports: [
    IonicPageModule.forChild(OrderFinalPage),
  ],
})
export class OrderFinalPageModule {}
